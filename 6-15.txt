关闭拼写检查，setting-->Inspections-->Spelling-->Typo，取消勾选

设置代理
    teamviewer+ccproxy

apt-get 只在 debian 或者 ubuntu 中才有，centos 等其他发行版是没有的

豆瓣爬虫
    403 setting添加user-agent  or def start_requests(self):  yield Request("http://www.techbrood.com/",headers={'User-Agent': "your agent string"})

    parse() 返回会在cmd 上显示
    yield  也会显示 -o json 输出 默认列表 包裹  迭代
    feed_export 中文乱码
        FEED_EXPORT_ENCODING = 'utf-8'

        要学会游泳，最重要的是你必
        须跳到水里去游！
        要想学会编程，你必须亲自动
        手编写代码！
        要编写代码，你需要一个顺手
        的工具……


        using System

        namespace SayHello
        {
            class Program
            {
                static void Main(string[], args)
                {
                    Console.WriteLine("Hello!");
                    Console.ReadKey(true);
                }
            }

        }
